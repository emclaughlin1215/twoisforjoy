<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Two is for Joy - Drupal and Rails Web Development" %></title>

    <link href="https://fonts.googleapis.com/css?family=Lato:400,400i,700|Special+Elite" rel="stylesheet">

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <a href="#" class="site_logo"></a>

      <%= link_to "Two is for Joy", root_path, class: "site_name" %>

      <nav>
        <%= link_to "Things What's Said", posts_path %>
        <%= link_to "Things What's Done", projects_path %>
        <%= link_to "Things What You Say", new_contacts_path %>
      </nav>
    </header>

    <%= yield %>

    <footer>
      <% if user_signed_in? %>
        <%= link_to "Log Out", destroy_user_session_path %>
      <% else %>
        <%= link_to "Log In", new_user_session_path %>
        <%= link_to "New Post", new_post_path %>
        <%= link_to "New Project", new_project_path %>
      <% end %>
    </footer>

  </body>
</html>
